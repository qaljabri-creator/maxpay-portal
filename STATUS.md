# MaxPay Portal — جرد الحالة

**تاريخ الجرد:** 15 سبتمبر 2026 — تحديث للجرد الأول (4 سبتمبر 2026)
**المصادر:** `maxpay-portal-spec.md` · `maxpay-finance-feedback-v2.md` · `README.md` · تاريخ git · فحص الكود الفعلي
**طريقة التحقّق:** قراءة الكود والقوالب والهجرات، وتشغيل المجموعتين والفحوص الأربعة، لا الاعتماد على التوثيق.

> **ملاحظة تسمية:** لا يوجد ملف باسم `FEEDBACK-V2.md` في المشروع. ملف مراجعة المالية هو
> `maxpay-finance-feedback-v2.md`، وهو ما جرى الاعتماد عليه.

---

## 0. الخلاصة في ثلاثة أسطر

المشروع **مكتمل وظيفياً إلى حدّ بعيد**: كل خطوات البناء الأربع عشرة في §12 من المواصفة منفّذة،
وكل بنود مراجعة المالية الاثني عشر منفّذة، و**1247 من 1248 اختبار Django** و**27 اختبار Node**
تمرّ. الإخفاق الوحيد ليس انحداراً بل **اختبار متذبذب** اكتُشف اليوم لأول مرّة — انظر §5.1.
ما يمنع النشر ليس ميزة ناقصة بل **بنية تشغيل شبه غائبة** (لا CI، ولا خادم تطبيقات، ولا Dockerfile)
و**قاعدة بيانات PostgreSQL لم تُشغَّل عليها سطر واحد قط**. وتوثيق `README.md` يحمل تناقضات
تُقلّل من موثوقيته كمرجع للحالة.

وبند جديد دخل القائمة منذ الجرد الأول: المستودع صار له مستودع بعيد، **لكن العمل كله بعده
لم يُدفَع إليه**. `origin/master` ما يزال عند الإيداع الأول وحده، والإيداعات الأربعة التي تحمل
إعادة بناء شاشة العميل وإصلاحات توكن B2CORE والمصافحة موجودة على فرع محلّي واحد على جهاز
واحد. انظر **B1-b** في §4.

وبند ثانٍ جديد، وهو الأهمّ فنياً في هذا الجرد: **مجموعة الاختبارات ليست حتمية.** ثلاثة
تأكيدات في `test_history.py` تقامر على أن مرجعاً عشوائياً لن يحوي الرقم الذي تبحث عنه، وفي
تشغيل اليوم خسرت الرهان. الاحتمال ~2.1% لكل تشغيل كامل. انظر **B14** في §4 و§5.1.
**(23 سبتمبر: أُغلق — المراجع صارت تُوزَّع من عدّاد يبدأ عند 30001 في `f104780`.)**

---

## 0.1 ما تغيّر منذ جرد 4 سبتمبر

أربعة إيداعات، كلها على الفرع `portal/one-screen-compose`، ولا شيء منها على `origin`:

| الإيداع | التاريخ | ما فيه | هل كان في الجرد الأول؟ |
| --- | --- | --- | --- |
| `ecfe016` | 4 سبتمبر | **شاشة تأليف واحدة بدل معالج من أربع خطوات.** الاختيارات الثلاثة (الاتجاه، التاجر، الطريقة) في صفّ واحد أعلى الشاشة والتفاصيل تحتها. `CHAIN` صار التصريح الوحيد بالترتيب؛ `advance()`/`retreat()`/`WIZARD`/شريط التقدّم كلها حُذفت | ✅ مذكور جزئياً |
| `aa6cd4d` | 4 سبتمبر | **قوائم منسدلة للاختيارات الثلاثة**، بأيقونة طريقة الدفع داخل الخيار نفسه، ولوحة مفاتيح كاملة (الأسهم، Home، End، Enter، Escape) | ✅ مذكور جزئياً |
| `f12a019` | 4 سبتمبر | **التحقق من التوكن الذي يصدره B2CORE فعلاً** — أربعة افتراضات خاطئة أُصلحت | ✅ مذكور |
| `b52a8c0` | **7 سبتمبر** | **المصافحة كما ينطقها B2CORE** — أربع فجوات، و`tests/js/` جديدة | ⛔ **غير مذكور إطلاقاً** |

### `b52a8c0` — المصافحة، وهو الجديد الوحيد على هذا الجرد

طه أكّد البروتوكول مقابل إطار Max UP عامل. أربع فجوات:

1. **`embed-jwt-token-error` لم يكن يُعالَج إطلاقاً.** الرفض كان يسقط من المستمع، فيجلس
   الإطار المهلة كاملة (15 ثانية) ثم يبلّغ `token_timeout` — أي **مشكلة شبكة، عن فشل
   مصادقة**. وأسوأ منه عند التجديد: الجلسة الميتة تبقى على الشاشة وقابلة للاستعمال طوال تلك
   الثواني الخمس عشرة بعد أن قال المضيف إنها انتهت. الآن `discardSession()` ثم
   `fail("token_rejected")` فور وصول الرفض، مع عرض السبب الذي سمّاه B2CORE.
2. **`expiresAt` لم يكن يُقرأ.** التجديد كان يجري على `expires_at` الخاص بخادمنا — وهو تلك
   القيمة نفسها مقصوصة على سقف الجلسة، أي **صحيح بالمصادفة**. يُقرأ الآن من الرسالة
   (ثوانٍ أو مللي ثانية أو ISO-8601) ويُجدول على أقرب الأجلين انقضاءً.
3. **التجديد كان يعيد المرحلة إلى `connecting`** فيسقط العميل من نموذج نصف ممتلئ — وهو
   بالضبط ما وُجد التجديد المبكر لمنعه. التجديد تحت جلسة حيّة صامت الآن.
4. **`event.origin` كان يُفحَص و`event.source` لا.** الأصل ليس نافذة: تبويب B2CORE ثانٍ،
   ونافذة منبثقة يفتحها، وإطار يستضيفه — كلها تتشارك الأصل. لا يتكلّم مع هذه الصفحة إلا
   `window.parent`.

وواحدة في الترتيب لا في المنطق: **`ready` كان يُرسَل بعد رحلة إلى خادمنا**. المضيف كان يُترك
منتظراً نداءً لا يعرف عنه شيئاً. `ready` يخرج أولاً الآن، ما يعني أن توكناً قد يصل والفحص ما
يزال في الطريق؛ **التوكن يفوز** والفحص يُسقَط بدل أن يصطفّ طلب ثانٍ.

**`tests/js/` مجموعة ثانية جديدة:** 27 اختباراً تُشغّل `static/js/embed.js` الحقيقي في سياق
`node:vm` مقابل DOM مكتوب يدوياً للعناصر الخمسة والمستمع الواحد و`fetch` والمؤقّتات التي
يلمسها ذلك السكربت. لا `node_modules` ولا خطوة بناء. **وهي بوّابة رابعة لا يشغّلها شيء
آلياً**، لأنه لا CI (B2).

### الثيم — نظام قائم لم يُجرَد من قبل

الثيم **ليس جديداً**: هو في الإيداع الأول، ولم يُذكر في الجرد الأول أصلاً. تحقّقتُ منه الآن لأنه
سُئل عنه، ووجدته من أنظف ما في المشروع:

- المضيف يعلن `embed-theme-change`، و`embed.js:279` يكتب `data-theme` على `<html>`،
  والخادم يحفظه (`session.py:SESSION_THEME`، `views.py:PreferencesView`) فيُعاد رسمه على
  `<html>` من القالب عند أي تحميل لاحق.
- `embed.css` يتفرّع على `data-theme` فقط. **لا `prefers-color-scheme` في أي مكان، عمداً:**
  الغرفة تقرّر لا نظام التشغيل. عميل على هاتف داكن ينظر إلى B2CORE فاتح يجب أن يرى إطاراً فاتحاً.
- الداكن يعيش في `system.css` وحده، والفاتح مكتوب كتجاوز له، فلا تُعاد قيمة واحدة مرّتين.
- `flow.css` **لا يسمّي لوناً واحداً**: 181 إشارة إلى `var(--…)` ولون واحد مثبَّت
  (`#fff` خلف صورة QR) موثَّق بسببه — الرمز المقلوب على أرض داكنة لا يقرأه أغلب الماسحات.
- **الفجوة:** لا اختبار ثيم في `tests/js/`. الجانب الخادمي مغطّى في `test_session.py`
  و`test_views.py`، أما كتابة `data-theme` من الرسالة فمؤكَّدة بالتشغيل اليدوي في Chrome لا باختبار.

---

## 1. المنجَز فعلاً في الكود، مقابل الموثَّق

### 1.1 ما تحقّقتُ منه في الكود ووجدته منجزاً

كل خطوات §12 من المواصفة، بالإضافة إلى خطوتين غير مرقّمتين فيها:

| الخطوة | الحالة | الدليل في الكود |
| --- | --- | --- |
| 1 — الهيكل والإعدادات والنماذج | ✅ | `config/settings/{base,dev,prod,env}.py`، 8 تطبيقات تحت `apps/` |
| 2 — الأدوار والصلاحيات و2FA | ✅ | `apps/accounts/permissions.py` (مصفوفة الصلاحيات)، `middleware.py` (فرض 2FA)، `throttling.py` |
| 3 — التجار والطرق والمحافظ | ✅ | `apps/merchants/models.py` (436 سطراً)، `lifecycle.py`، شاشات في `apps/finance/views.py` |
| 4 — أسعار الصرف مع التاريخ | ✅ | `apps/rates/models.py` — نموذج غير قابل للتعديل، `RateListView`/`RateCreateView` |
| 5 — التحقق من JWT الخاص بـ B2CORE | ✅ | `apps/portal/b2core/{jwks,tokens}.py`، `session.py`، `sessions.py`، و`static/js/embed.js` + `tests/js/` للمصافحة |
| 6 — تدفّق الإيداع للعميل | ✅ | `apps/portal/{catalog,pricing,submissions,payloads,flow_views}.py`، `static/js/flow.js` |
| 7 — طابور المالية والإسناد والاعتماد | ✅ | `apps/finance/queue_views.py` (543 سطراً)، `apps/transactions/services.py` (936 سطراً) |
| 8 — لوحة التاجر بمُسلسِلات مُقنَّعة | ✅ | `apps/merchant_panel/{anonymity,serializers,scoping,api}.py` |
| 9 — محادثات الطلبات مع إشراف المالية | ✅ | `apps/transactions/messaging.py` (312 سطراً) |
| 10 — تدفّق السحب | ✅ | `apps/portal/destinations.py`، مسارات السحب في `submissions.py`، `test_withdrawal.py` |
| 11 — ساعات العمل والعدّ التنازلي | ✅ | `apps/core/hours.py` (202 سطراً)، `finance:business_hours`، عدّاد في `flow.js` |
| 12 — سجل التدقيق وعارضه | ✅ | `apps/core/models.py:AuditLog` + هجرة المُشغِّلات، `apps/finance/audit_views.py` |
| 13 — التحديث الحي بالاستطلاع | ✅ | `apps/finance/live_views.py`، `apps/merchant_panel/api.py:MerchantPulseAPI`، `static/js/panel.js` |
| 14 — التقسية الأمنية ومجموعة الاختبارات | ✅ | `apps/core/{checks,middleware}.py`، `apps/portal/checks.py`، 1248 اختبار Django + 27 اختبار Node |
| 15 — المستخدمون والأدوار (خارج ترقيم المواصفة) | ✅ | `apps/finance/user_views.py`، `apps/accounts/provisioning.py` (449 سطراً) |
| 16 — التقارير والتصدير (خارج ترقيم المواصفة) | ✅ | `apps/reports/` (`filters`, `aggregates`, `workbook`) + شاشتان في اللوحتين |
| 17 — الثيم واللغة تبعاً للمضيف (§4 خطوة 6) | ✅ | `embed.css` يتفرّع على `data-theme` وحده، `session.py:set_theme/get_theme`، `views.py:PreferencesView`. **لم يُجرَد سابقاً** — انظر §0.1 |
| 18 — مصافحة الإطار كما يتكلّمها B2CORE | ✅ **بعد 7 سبتمبر** | `static/js/embed.js`، و`tests/js/embed_handshake.test.js` (27 اختباراً). انظر §0.1 |

**إخفاء هوية العميل (§2)** — أقوى ما في المشروع، وثلاث طبقات مستقلة تحرسه، تحقّقتُ منها جميعاً:

1. `apps/accounts/permissions.py:MERCHANT_FORBIDDEN` — عشر صلاحيات هوية يرفض `sync_role_groups()`
   بناء مجموعة التاجر إن مُنحت أياً منها. مخرجات تشغيل الاختبارات تؤكّد ذلك حرفياً:
   `Client anonymity check passed: merchant holds none of the 10 identity permissions.`
2. `apps/merchant_panel/anonymity.py` — فحص أسماء الحقول عند تعريف المُسلسِل، وعند التسلسل،
   وعند الاستجابة. 24 مفتاحاً محظوراً و13 سلسلة فرعية محظورة.
3. `apps/merchant_panel/tests/test_api.py` — يمشي على URLconf لوحة التاجر ويكنس كل استجابة
   بحثاً عن الهوية **بالقيمة** لا بالاسم فقط.

**بنود مراجعة المالية (12 بنداً):** كلها منفّذة. تحقّقتُ من كلٍّ منها في الكود:

| البند | الحالة | الدليل |
| --- | --- | --- |
| 1.1 حذف إثبات الدفع من سحب العميل | ✅ **بعد إصلاح 4 سبتمبر** — كان معطوباً | كان `needs.proof=false` صحيحاً والحقل يظهر رغم ذلك. انظر §1.3 أدناه |
| 1.2 إزالة الكسور العشرية | ✅ | `apps/portal/pricing.py`: `DINAR = Decimal("1")` + `ROUND_HALF_UP`، ونفس الترتيب في `flow.js` |
| 1.3 تحديث حي للمحادثة | ✅ | `static/js/flow.js:1508–1566` — استطلاع كل 10ث، توقيع للحمولة، إيقاف عند إخفاء التبويب، تراجع تدريجي |
| 1.4 صورة/QR لكل محفظة | ✅ | `Wallet.qr_image` (هجرة `0004`)، `PaymentMethod.requires_wallet_number` (هجرة `0003`)، مسار `portal:wallet_qr` |
| 2.1 إعادة الطلب من التاجر | ✅ | `services.py:TRANSITIONS["hand_back"]` — سبب إلزامي، ملاحظة داخلية، `clears_merchant=True` |
| 2.2 الإلغاء مقابل الرفض | ✅ | `RequestStatus.CANCELLED` + انتقال `cancel` مستقل عن `reject`، صلاحية `cancel_request` منفصلة |
| 2.3 إعادة الإسناد بعد الإرجاع | ✅ | `route` يقبل `PENDING` ضمن `sources` |
| 2.4 حالة «معلّق» صريحة | ✅ | `RequestStatus.PENDING`، وهي **غير** مدرجة في `TERMINAL_STATUSES` عمداً |
| 3.1 تعديل المبلغ | ✅ | `services.py:correct_amount` (سطر 626)، `submitted_amount_usd/iqd`، `AuditAction.AMOUNT_CHANGE` (هجرة `0003`) |
| 3.2 حقول التوقيت | ✅ | `Request.resolved_at`، `elapsed`، `elapsed_display` |
| 3.3 نسبة العملية للمنفّذ | ✅ | `Request.handled_by` + `handled_at` (هجرة `0005`)، `_stamp_operator` |
| 3.4 الفرز بتاريخ الحسم | ✅ | مرشّحات الطابور في `queue_forms.py` |
| 3.5 ملاحظة التاجر عند التحويم | ✅ | `queue_views.py:76 LATEST_MERCHANT_NOTE` (Subquery)، و`_queue_rows.html:21 title="{{ req.merchant_note }}"` |
| 3.6 عنوان قابل للتعديل | ✅ | `Request.title` يخزّن التجاوز فقط + `display_title` |
| 4.1 سجل العميل الكامل | ✅ | `apps/finance/client_views.py` خلف `accounts.view_client_identity`، `test_history.py` (414 سطراً) |
| 4.2 البحث | ✅ | `apps/finance/search.py:as_amount` — يبحث في المبلغ الحالي والمبلغ الأصلي معاً |
| 4.3 السجل في التقارير | ✅ | `ReportFilter.client` + `finance:report?client=…` |

### 1.2 التعارضات بين الكود والتوثيق

**سبعة من ثمانية في اتجاه واحد: `README.md` يقلّل من قيمة ما بُني.** والثامن — البند 8 أدناه —
في الاتجاه المعاكس، وهو الوحيد من نوعه: ادّعاء إنجاز لم يكن قائماً.

| # | ما يقوله التوثيق | ما يقوله الكود | الخطورة |
| --- | --- | --- | --- |
| 1 | `README.md:7` — «**Build status: steps 1–9 of the spec's build order are complete.**» | متن الملف نفسه يوثّق الخطوات 10 و11 و12 و13 و14 و15 و16، والكود يؤكدها. السطر 7 **قديم ومضلّل** | عالية — أول سطر يقرأه أي شخص |
| 2 | `README.md:2755` — «**Nothing polls the client's request view.**» | خطأ، **وما يزال قائماً في 15 سبتمبر**. `static/js/flow.js:1945–1996` (انتقل من 1508 بعد إعادة البناء) يستطلع كل 10ث: توقيع للحمولة يمنع إعادة الرسم بلا تغيير، وتوقّف عند إخفاء التبويب، وتراجع تدريجي عند الفشل. والبند يزعم أن التمديد «deliberately not done» — وهو موصوف بالتفصيل في تعليق الملف نفسه | عالية — بندان في نفس الملف يتناقضان |
| 3 | `README.md:2678` — «**A permission cannot be revoked from one user.**» | خطأ. `User.denied_permissions` (هجرة `accounts/0002`) + الطرح في `ThrottledModelBackend.get_all_permissions`. و`README.md:1599` يشرح التنفيذ بالتفصيل | عالية |
| 4 | ~~`README.md:2496` — «**Nothing has been exercised in a browser.**»~~ — **أُغلق** | الجملة اختفت. محلّها الآن فقرة دقيقة (`README.md:2766–2780`) تقول ما شُغِّل في Chrome في 4 سبتمبر وما لم يُشغَّل، وتسمّي الثلاثة التي تعتمد على المضيف بالاسم. تحقّقتُ من النصّ الجديد ووجدته مطابقاً للواقع | ~~متوسطة~~ |
| 5 | `README.md:69` — «**and the middle one is not optional in CI**» | **لا يوجد CI.** لا `.github/`، لا `.gitlab-ci.yml`، لا `Jenkinsfile` — تحقّقتُ مجدداً في 15 سبتمبر. الجملة عُدِّلت في 7 سبتمبر إلى «The first three» لاستيعاب بوّابة Node رابعة، **وهو تعديل يزيد الادّعاء ولا ينقصه**: صارت أربعة أوامر يُقال إنها تجري في CI غير موجود | عالية |
| 6 | `README.md:59` — «Both `devdata/` and `static/dev/` are **git-ignored**» | ~~كان صحيحاً في `.gitignore` بلا مستودع~~ — **أُغلق:** المستودع أُنشئ في 4 سبتمبر وتحقّقتُ أن كليهما غير متعقَّب فعلاً | ~~عالية~~ |
| 7 | `services.py:641` — «Finance was **asked**… and said no» | `maxpay-finance-feedback-v2.md` يدرج هذا البند تحت «**Decisions needed before building**» بصيغة توصية لا قرار. لا يوجد في المستودع ما يوثّق موافقة المالية. **تحقّقتُ مجدداً في 15 سبتمبر: التعليق كما هو** | متوسطة — تعليق كود يدّعي موافقة غير موثّقة |
| 8 | `README.md` (سابقاً) — البند 1.1: «the field is hidden… **Nothing was changed**» | **خطأ، والاتجاه معاكس لكل ما سبق.** الحقل كان يظهر فعلاً في شاشة السحب، والخادم كان يقبل إثباتاً مُرسَلاً ويهمله بصمت. أُصلح في 4 سبتمبر — انظر §1.3 | **عالية — كانت خللاً في الإنتاج، لا في التوثيق** |

### 1.3 البند 1.1: لماذا كان معطوباً رغم أن كل منطقه صحيح

**العرض.** `static/js/flow.js:994` يخفي الحقل بـ `node.hidden = true`، لكن
`static/css/flow.css:381` كان يعرّف `.field { display: flex }` **بلا قاعدة `[hidden]` مقابلة**.
قاعدة المؤلّف تتقدّم على قاعدة متصفّح المستخدم `[hidden] { display: none }` مهما كانت النوعية،
فالسمة `hidden` لم تكن تفعل شيئاً وكل نداء `show(node, false)` على حقل كان بلا أثر.

العيب **لم يكن خاصاً بالإثبات**: الشاشة الرابعة شاشة واحدة تخدم اتجاهين منذ الخطوة 10، وذات
الخلل كان يُظهر `#destination-field` — رقم بطاقة العميل — في تدفّق **الإيداع**. سطر واحد يصلح
الاثنين. والدليل على أنه سهو لا تصميم: الملف يحمل قاعدة `[hidden]` مقابلة لاثني عشر مكوّناً
آخر، منها `.field__error` و`.field__review` — وهما **ابنا** الصنف الذي نُسي.

**القبول.** `build_withdrawal_draft` لم يكن يقرأ `files` إطلاقاً، فالملف لم يكن يُخزَّن — لكنه لم
يكن يُرفض أيضاً. الطلب يُنشأ والملف يسقط بصمت. من جهة العميل هذا لا يُميَّز عن إثبات حُفِظ،
وهو أسوأ الفشلين: أرفق دليلاً، ونجح الإرسال، ولا شيء يُقدَّم عند نزاع على الدفع.

**هل عاد مع تعديل لاحق؟** عملياً نعم، والعرض هو ما عاد. منطق البند 1.1 سليم بالكامل في الخادم
وفي `flow.js`؛ ما كسره تعديل في CSS. تعذّر إثبات ذلك بالتأريخ لأن المستودع لم يكن قد أُنشئ بعد، وهذا
بالضبط ما يكلّفه غياب تحكّم الإصدارات: سؤال «متى دخل هذا» لا جواب له.

**الإصلاح:** `.field[hidden] { display: none; }` في `flow.css`، ورفض صريح
(`proof_not_accepted`) في `submissions.py:refuse_proof` يسبق كل حقول الشاشة، وتوجيه للرمز
الجديد في `flow.js` كي لا يُعرض الرفض على سطر خطأ حقلٍ مخفيّ. الاختبارات:
`HidingWhatIsNotCollectedTests` و`WithdrawalProofIsRefusedTests` في
`apps/portal/tests/test_withdrawal.py` — تحقّقتُ من أن كلاً منهما يفشل إن أُعيد الخلل.

---

## 2. الناقص أو المنفَّذ جزئياً

### 2.1 ناقص فعلاً (فجوات في الميزات)

> **حالة القسم في 15 سبتمبر: الستّة كما هي، بلا حركة.** الإيداعات الأربعة لم تلمس
> `audit_views.py` ولا `transactions/` ولا `reads.py` ولا `core/hours.py`، وقيم
> `PORTAL_DESTINATION_{MIN,MAX}_DIGITS` كما كانت — تحقّقتُ من كلٍّ منها.

| # | الفجوة | الأثر | المرجع |
| --- | --- | --- | --- |
| 1 | **لا تصدير لسجل التدقيق.** لا مسار تصدير في `apps/finance/audit_views.py`؛ العارض فقط | مدقّق يريد الملف يقع على list view في Django admin | §9 يطلب عارضاً فقط، فهذه إضافة لا نقص متعاقَد عليه |
| 2 | **لا يمكن للتاجر رفع إثبات خارج انتقال `pay`.** الرفع مربوط بالانتقال، لا كإجراء مستقل | لا يمكن إضافة مستند ثانٍ بعد الدفع | §8 يذكر «upload proof» كإجراء مستقل |
| 3 | **شارة عدم القراءة تعدّ الطلبات لا الرسائل.** طلب بثلاث رسائل غير مقروءة يُحسب مرّة | §10 يطلب «badge»، والحبيبة الحالية مقبولة لكنها اختيار غير مؤكَّد | `apps/transactions/reads.py` |
| 4 | **ساعات العمل نافذة واحدة لكل يوم، لكل الأيام.** لا عطلة أسبوعية ولا عطلة رسمية ولا جدول لكل يوم | تُغلق المالية يوم الجمعة برمي تجاوز يدوي يجب على أحدهم إرجاعه | `SystemSettings` في §5 لا يعطي أكثر من زوج واحد `open_time`/`close_time` |
| 5 | **لا فحص لرصيد العميل قبل تقديم السحب** | يقدّم العميل سحباً يفوق رصيده ولا يعرف إلا عند الرفض | §6 يضع الفحص على المالية عند `under_review`، و§13 يستثني وصول B2CORE API |
| 6 | **حساب الوجهة يُفحص شكلاً لا صحةً.** أرقام فقط ونطاق طول واسع (6–32 رقماً) | لا شيء يميّز حساباً صحيحاً من حساب شخص آخر | `PORTAL_DESTINATION_{MIN,MAX}_DIGITS` |

### 2.2 منفَّذ لكن على قيم مؤقّتة

| البند | القيمة الحالية | المطلوب |
| --- | --- | --- |
| حدود الإيداع | `PORTAL_DEPOSIT_MIN_USD=1` / `MAX=100000` | «placeholders that only stop obvious nonsense» — تأكيد من المالية |
| حدود السحب | `PORTAL_WITHDRAWAL_MIN_USD=1` / `MAX=100000` | نفس الشيء، والاتجاهان غير متماثلين عمداً |
| اتجاه التحويل | العميل يدخل USD ويرى IQD | `apps/portal/pricing.py` هو المكان الوحيد الذي يتغيّر لو أرادت المالية العكس |
| عمولة السحب | تُخصم من المبلغ المدفوع، وتظهر «العمولة (تُخصم)» | §5 لا يقول في أي اتجاه تشير، والخصم استنتاج |

### 2.3 منفَّذ لكن غير مُختبَر على البيئة المستهدَفة

| البند | الحالة |
| --- | --- |
| **PostgreSQL** | **لم يُشغَّل عليه شيء قط.** كل الهجرات وكل الاختبارات على SQLite فقط. لا يوجد docker في هذه البيئة، فتعذّر التحقق. القيد الجزئي على `Wallet` وقيد `CheckConstraint` على `PaymentMethod` وحقول `JSONField` ومُشغِّلات `plpgsql` لسجل التدقيق — كلها لم تُنفَّذ مرة واحدة على المحرّك المستهدَف |
| **B2CORE الحقيقي** | **تحسّن مرّتين.** في 4 سبتمبر فُحصت حمولة توكن حقيقي فتبيّن أن أربعة افتراضات كانت خاطئة: الخوارزمية `EdDSA` لا RS256، لا وجود لـ `aud` إطلاقاً، `iss` على `api.*` بمسار وشرطة مائلة أخيرة لا أصل مجرّد، والاسم يأتي `first_name`/`last_name` بلا مطالبة مركّبة. الأربعة أُصلحت ومغطّاة باختبارات من الحمولة المرصودة، و`portal.W006` صار `E006` لأنه كان **يعطي نصيحة تُسقط الإنتاج**، و`prod.py` صار يرفض الإقلاع بلا مُصدِر. وفي 7 سبتمبر أكّد طه البروتوكول مقابل إطار Max UP عامل، فأُغلقت أربع فجوات في `embed.js` وأضيفت `tests/js/`. **وما يزال غير مُجرَّب**: تبادل postMessage لم يجرِ مرّة واحدة مقابل بوابة B2CORE حقيقية — كل الـ27 اختباراً تجري مقابل مضيف مُقلَّد في `node:vm` — وجلب JWKS ما يزال مُزيَّفاً في المجموعة. أسماء الرسائل **حُسمت**، لكن **شكل الغلاف ما يزال متسامحاً عمداً**: `type\|event\|action\|name` للاسم و`payload\|data\|value` للقيمة |
| **المتصفّح** | **تحسّن جزئياً في 4 سبتمبر.** شاشة العميل شُغِّلت فعلاً في Chrome ضد بيانات `seed_demo`، **في الوضعين الفاتح والداكن**: صفّ القوائم المنسدلة الثلاث بمحاذاة واحدة مؤكَّدة بالقياس (نفس `top` ونفس الارتفاع للثلاثة)، أيقونات طرق الدفع داخل الخيارات نفسها، لوحة المفاتيح كاملة (الأسهم وHome وEnd وEnter وEscape مع إعادة التركيز)، فتح الأعمدة بالتسلسل، القائمة المقفلة معطَّلة **ومفرَّغة**، إعادة الضبط عند تغيير اختيار سابق، إخفاء الإثبات في السحب (`display: none` مؤكَّد حيّاً)، تقديم طلب كامل حتى المرجع، والانهيار لعمود واحد عند 400px — بلا خطأ واحد في الـ console. **لكن ليس داخل إطار B2CORE حقيقي**: زر النسخ (يحتاج `clipboard-write` من المضيف)، ارتفاع الإطار على طلب طويل، ولوحة المفاتيح الرقمية — ما تزال غير مؤكَّدة. و`panel.js` (استطلاع اللوحتين) لم يُشغَّل في متصفّح بعد. **جديد في 7 سبتمبر:** `embed.js` صار مغطّى بـ27 اختباراً في Node، وهي أول تغطية آلية لسطر JavaScript في هذا المشروع — لكنها تغطية سلوك السكربت، لا تغطية متصفّح |
| **`tests/js/` بحدّ ذاتها** | المجموعة تعمل (27/27) ولا شيء يشغّلها إلا يد. ليست ضمن `manage.py test`، وليست في CI لأنه لا CI. أوّل مرة يُكسر فيها `embed.js` بلا تشغيلها يدوياً ستمرّ صامتة |
| **الثيم** | الجانب الخادمي مغطّى (`test_session.py`، `test_views.py`)، والتبديل الحيّ **مؤكَّد بالتشغيل اليدوي في Chrome في 4 سبتمبر لا باختبار**. `tests/js/` — وهي الوحيدة التي تستطيع اختبار كتابة `data-theme` من رسالة المضيف — **لا تحوي اختبار ثيم واحداً**. وما لم يُختبر إطلاقاً: الثيم داخل إطار B2CORE حقيقي، أي مصدر القيمة كلها |
| **حتمية المجموعة نفسها** | **ليست حتمية — اكتُشف في 15 سبتمبر.** ثلاثة تأكيدات في `apps/finance/test_history.py` تبحث بمصطلح رقمي قصير (`"250"`، `"777"`) وتتوقّع نتيجة واحدة، بينما `public_ref` عشوائي و`search.query()` يطابقه دائماً بـ`icontains`. ~2.1% احتمال إخفاق لكل تشغيل. **كل جملة «كل الاختبارات تمرّ» في هذا الملف وفي `README` وفي رسائل الإيداع تحمل هذا التحفّظ بأثر رجعي** |
| **استعادة النسخة الاحتياطية** | `manage.py backup_database` موجود ولم يُستعَد منه قط |

---

## 3. القرارات المعلّقة

> **حالة القسم في 15 سبتمبر:** من الأحد عشر يوماً الماضية خرج **جواب واحد**، وهو من طه لا من
> المالية: بروتوكول المصافحة (§3.3 البند 1). **لم يصل جواب واحد من المالية** — لا على الأربعة
> المنفَّذة بالتوصية في §3.1، ولا على الأحد عشر سؤالاً في §3.2. البند 11 في §3.2، وهو الذي
> فرضه الواقع ويحجب القيد اليدوي في الـ Back Office، مرّ عليه أحد عشر يوماً بلا ردّ.

### 3.1 بانتظار المالية — قرارات نُفِّذت بالتوصية ولم تُؤكَّد كتابةً

كل القرارات الأربعة في صدر `maxpay-finance-feedback-v2.md` **نُفِّذت بالفعل** حسب توصية الفريق
التقني. لا شيء متوقّف بانتظارها، لكن أيّ اعتراض متأخّر من المالية يعني إعادة عمل.

| # | القرار | ما نُفِّذ | مستوى الخطر لو اعترضت المالية |
| --- | --- | --- | --- |
| 1 | **قاعدة التقريب** | الخيار الثالث: USD يحتفظ بالسنتات، IQD بالدينار الصحيح، السعر بمنزلتين. `README.md:1671` يقرّ صراحةً بأنه «كان اختياراً» | **متوسط** — التغيير في `pricing.py` و`flow.js` فقط، والصفوف القديمة لا تُهاجَر عمداً |
| 2 | **إعادة التسعير عند تعديل المبلغ** | السعر لا يتحرّك، والعمولة يُعاد توزيعها نسبياً من `commission_rate_applied`. تعليق `services.py:640` يزعم أن المالية «سُئلت وقالت لا» — **وهذا غير موثّق في أي مكان** | **عالي** — أموال. لو أرادت المالية إعادة التسعير بسعر اليوم فالمنطق كله يتغيّر |
| 3 | **نموذج الإلغاء من التاجر** | الخيار (ب): `hand_back` → `PENDING` بملاحظة داخلية إلزامية، والمالية تعيد الإسناد أو تلغي | **متوسط** — الخيار (أ) يحتاج حالة موافقة فرعية جديدة |
| 4 | **سجل العميل والإخفاء** | للمالية فقط، خلف `accounts.view_client_identity`، والعرض يرفض تماماً لمن لا يملكها | **منخفض** — التوصية كانت قاطعة ولا بديل معقول |

### 3.2 بانتظار المالية — لم يُسأل عنها بعد

| # | السؤال | لماذا يهم |
| --- | --- | --- |
| 1 | **حدود الإيداع والسحب الحقيقية بالدولار** | القيم الحالية `1` و`100000` مؤقّتة صراحةً |
| 2 | **اتجاه التحويل**: هل يُدخل العميل USD أم IQD؟ | ما بُني هو USD→IQD، وهو ما يقرأه §7 كذلك |
| 3 | **`Wallet.daily_cap`**: هل يعدّ الإجمالي المحوَّل (بالعمولة) أم المبلغ المحوَّل وحده؟ ومتى يدور اليوم — منتصف الليل المحلي أم قطع متّفق عليه مع التجار؟ | السقف مفروض بالفعل بالدينار؛ الوحدة حُسمت والحبيبة لم تُحسم |
| 4 | **اتجاه عمولة السحب**: تُخصم من المدفوع أم تُضاف؟ | تُخصم حالياً، وهو استنتاج. البديل ليس ظاهر الخطأ لمن يقرأ طلباً واحداً |
| 5 | **توقيت خصم رصيد B2CORE في السحب** (§6 يسمّيه «Open decision») | مُنفَّذ عند `under_review`، وهو ما يصفه جدول المواصفة نفسه. نقله إلى `assigned` = سلسلتان نصّيتان وحالة مصدر |
| 6 | **ساعات عمل مختلفة لكل يوم؟** | لو نعم فهذا نموذج ثانٍ. يُستحسن حسمه قبل أول عيد لا بعده |
| 7 | **تصدير سجل التدقيق: من يحقّ له؟** | أول طريق يخرج به السجل من النظام، فيحتاج قراراً لا مجرد كود |
| 8 | **شارة عدم القراءة: عدد الطلبات أم عدد الرسائل؟** | `reads.py` هو المكان الوحيد الذي يتغيّر |
| 9 | **عرف مكتوب لعدم كتابة اسم العميل في الرسائل** | لا يمكن لأي نظام منع موظّف مالية من كتابة اسم عميل في ردّ. §9 وضع تذكيراً دائماً على صندوق الردّ، وهو أقصى ما يفعله الكود. وهل يُقبل أن **يعرّف العميل بنفسه** للتاجر؟ اليوم يُقبل، وهو طريق الإفشاء الوحيد الذي لا يغلقه التصميم |
| 10 | **صيغ حسابات لكل طريقة دفع؟** (11 رقماً لـ ZainCash، 16 لبطاقة) | تحتاج `PaymentMethod` أن تحمل القاعدة، وقاعدة خاطئة ترفض عملاء شرعيين |
| 11 | **كيف تربط المالية الطلب بحساب B2CORE للقيد اليدوي؟** | **هذا سؤال جديد فرضه الواقع، وهو الأهمّ في القائمة.** توكن B2CORE لا يحمل رقم حساب ولا نوع عميل، فحقل `Client.account_number` فارغ لكل عميل توثّق منذ 4 سبتمبر. البريد صار هو المُعرِّف: عليه تبحث المالية، وبه تربط. لكن §13 يفترض قيداً يدوياً في الـ Back Office، والسؤال العملي — هل البريد كافٍ للعثور على الحساب هناك، أم أن الربط يحتاج `sub` (وهو UUID تحمله `Client.b2core_id` ويُبحث به أيضاً)؟ **لم نملأ الحقل بـ `sub` ولا `sid`**: كلاهما مُعرِّف حقيقي وليس رقم حساب، وملء عمود تقرؤه المالية «رقم الحساب» بشيء ليس كذلك أسوأ من فراغ ظاهر |

### 3.3 بانتظار الفريق التقني / B2CORE

| # | البند | ما يحتاجه |
| --- | --- | --- |
| 1 | ~~**شكل حمولة `postMessage` غير موثّق لنا**~~ — **حُسم جزئياً في 7 سبتمبر** | طه أكّد البروتوكول مقابل إطار Max UP عامل. **الأسماء صارت ثوابت**: `embed-iframe-ready`، `embed-request-jwt-token`، `embed-jwt-token`، `embed-jwt-token-error`، `embed-logout`، `embed-theme-change`، `embed-language-change`. وأُضيف `expiresAt` بجانب التوكن. **ما بقي مفتوحاً عمداً:** شكل الغلاف — `messageType` ما يزال يقبل `type\|event\|action\|name` و`messageValue` يقبل `payload\|data\|value` — والتعليق يبرّره بأن بناءات B2CORE تختلف وأن الحارس الحقيقي هو فحص الأصل والنافذة لا اسم المفتاح. قرار معقول، لكنه يبقى **غير مضيَّق مقابل مضيف إنتاجي** |
| 2 | ~~**أسماء الادعاءات في الرمز**~~ — **حُسمت بالرصد** | `sub` و`email` و`first_name`/`last_name` و`aal`/`amr`/`sid`/`jti`. `iss` مثبَّت على القيمة الحرفية، و`aud` **لا وجود له** ويجب أن يبقى فارغاً. **لا رقم حساب ولا نوع عميل** — وهذا هو البند 5 في §3.4 |
| 3 | **إنشاء معاملات B2CORE آلياً** | خارج النطاق في §13 «pending API access confirmation». اليوم القيد والخصم يدويّان في الـ Back Office، والنظام يملك التعليمة فقط |
| 4 | **قيم B2CORE الإنتاجية** (ضاق نطاقه) | `B2CORE_JWT_ISSUER` صار معروفاً ومضبوطاً افتراضياً على `https://api.maxifyfx.com/srvsz/auth/clients/v1/`. ما يزال مجهولاً: **عنوان JWKS الحقيقي** — تعليق الكود كان يفترضه في جذر `api.*` بينما المُصدِر يحمل مسار `/srvsz/auth/clients/v1/`، فالأرجح أنه تحته. و`B2CORE_ORIGIN` الإنتاجي |

---

## 4. ما يمنع النشر للإنتاج تحديداً

مرتّبة بحسب ما يوقف النشر أولاً.

### لوحة الحواجز التسعة الأصلية — أين صارت في 15 سبتمبر

| الحاجز | 4 سبتمبر | 15 سبتمبر | ما تحرّك |
| --- | --- | --- | --- |
| **B1** تحكّم الإصدارات | ⛔ | ✅ **مُغلق** | المستودع أُنشئ، ومستودع بعيد صار موجوداً |
| **B2** لا CI | ⛔ | ⛔ **مفتوح** | لا شيء. وصارت البوّابات أربعاً بدل ثلاث |
| **B3** لا PostgreSQL | ⛔ | ⛔ **مفتوح** | لا شيء. لا docker ولا psql ولا منفذ 5432 |
| **B4** لا مكدّس تشغيل | ⛔ | ⛔ **مفتوح** | 23 سبتمبر: `gunicorn==26.2.0` صار في `requirements.txt`، ودليل النشر `TAHA.md` يكتب systemd وnginx. لم يُشغَّل شيء منها بعد، ولا Dockerfile |
| **B5** `REDIS_URL` مكسور | ⛔ | ✅ **مُغلق** | `f104780`: `redis==8.1.0` في `requirements.txt`، و`REDIS_URL` مضبوط في `.env.example`، وRedis في `docker-compose.yml` |
| **B6** لا اختبار اختراق | ⛔ | ⛔ **مفتوح** | لا شيء |
| **B7** الاستعادة لم تُجرَّب | ⛔ | ⛔ **مفتوح** | لا شيء |
| **B8** `BACKUP_DIR` والجدولة | ⛔ | ⛔ **مفتوح** | لا شيء |
| **B9** JWKS والأصل | ⛔ | 🔸 **ضاق ولم يُغلق** | أسماء المصافحة حُسمت (طه). يبقى عنوان JWKS و`B2CORE_ORIGIN` |

**واحد أُغلق من تسعة، واثنان جديدان فُتحا: B1-b (لا شيء مدفوع إلى `origin`) وB14 (المجموعة
متذبذبة).** كل ما تحرّك في الأحد عشر يوماً الماضية كان في الكود — وهو الجانب الذي لم يكن يمنع
النشر أصلاً. **بنية التشغيل لم تتقدّم خطوة واحدة.**

### حاجزة — لا يمكن النشر قبلها

| # | الحاجز | التفصيل |
| --- | --- | --- |
| ~~**B1**~~ | ~~**لا يوجد تحكّم بالإصدارات إطلاقاً**~~ — **أُغلق في 4 سبتمبر 2026** | كان أخطر بند في الجرد. المستودع أُنشئ بإيداع أول (`c42cddb`) يضمّ المشروع كاملاً، و`.gitignore` يستثني `.env` وقواعد البيانات و`devdata/` و`private_media/` — تحقّقتُ مجدداً في 15 سبتمبر عبر `git ls-files`: لا سرّ واحد متعقَّب. ومستودع بعيد صار موجوداً: `github.com/qaljabri-creator/maxpay-portal` |
| **B1-b** | **لا شيء بعد الإيداع الأول دُفِع إلى `origin`** — **بند جديد، 15 سبتمبر** | `origin/master` و`master` كلاهما عند `c42cddb`. الإيداعات الأربعة التالية — إعادة بناء شاشة العميل، القوائم المنسدلة، إصلاحات توكن B2CORE، والمصافحة — تعيش على `portal/one-screen-compose` على جهاز واحد فقط. **هذا يعيد فتح نصف ما أغلقه B1**: التاريخ موجود، والنسخة الاحتياطية خارج الجهاز ليست. قرص واحد يُفقد فيضيع كل ما بعد 4 سبتمبر الساعة 11:15. والفرع لم يُراجَع ولم يُدمَج |
| **B2** | **لا CI** | `README.md:69` يقول إن `check --deploy` «not optional in CI». لا `.github/` ولا أي ملف CI — تحقّقتُ مجدداً في 15 سبتمبر. الاختبارات تُشغَّل يدوياً فقط، **وصارت أربع بوّابات بدل ثلاث** بعد `tests/js/`. وكل إيداع جديد يزيد ما يمكن أن ينكسر بلا أن يلاحظه أحد |
| **B3** | **لم يُشغَّل شيء على PostgreSQL قط** | وهي قاعدة البيانات التي تستهدفها المواصفة. الهجرات ومُشغِّلات `plpgsql` لسجل التدقيق (§11 «append-only») والقيد الجزئي على `Wallet` — لم يُنفَّذ أيٌّ منها مرة واحدة على المحرّك الحقيقي. مسار SQLite في هجرة `core/0002` مختبَر، ومسار PostgreSQL **غير مختبَر**. تحقّقتُ في 15 سبتمبر: لا `docker` ولا `psql` في المسار، والمنفذ 5432 مغلق. **أحد عشر يوماً مرّت ولم يقترب هذا البند خطوة** |
| **B4** | **لا مكدّس تشغيل** | لا `Dockerfile`، لا `Procfile`، لا `gunicorn` ولا `uvicorn` في `requirements.txt`، لا `whitenoise` ولا أي استراتيجية لخدمة الملفات الساكنة (`STATIC_ROOT` معرَّف ولا شيء يخدمه)، لا إعداد وكيل عكسي. `docker-compose.yml` يحتوي على **خدمة قاعدة بيانات فقط** |
| ~~**B5**~~ ✅ | **مُغلق في `f104780`.** ما يلي هو الوصف الأصلي، للتاريخ. **ملاحظة:** الإغلاق في الكود — أي بيئة تُبنى من `requirements.txt` تحمل `redis`؛ بيئة مطوّر قديمة لم تُعِد `pip install -r requirements.txt` لا تحمله. **`REDIS_URL` مكسور كما هو** | `core.W010` يحذّر أن `LocMemCache` لكل عملية، والعلاج الموثّق هو `REDIS_URL`. لكن حزمة `redis` **ليست في `requirements.txt`** ولا في البيئة الافتراضية — تحقّقتُ مجدداً في 15 سبتمبر: `ModuleNotFoundError: No module named 'redis'`. أي نشر بأكثر من عامل واحد إمّا يُشغّل مُحدِّدات معدّل مكسورة، أو ينهار عند أول وصول للذاكرة المؤقتة |
| **B6** | **لا اختبار اختراق طرف ثالث** | §11، السطر الأخير. بوّابة لا يغلقها أي قدر من الكود |
| **B7** | **الاستعادة من النسخة الاحتياطية لم تُجرَّب** | §11 يطلب «restore tested before go-live». الأمر موجود (`backup_database`)، والاستعادة لم تحدث مرة |
| **B8** | **`BACKUP_DIR` غير معيَّن ولا جدولة** | `core.W011` يُطلقها. §11 يطلب نسخاً **يومياً آلياً**؛ لا cron ولا جدولة في أي مكان. والأمر نفسه **لا يشفّر ولا يشحن الملف خارج المضيف** — موثَّق صراحةً في رأس `backup_database.py` |
| **B9** | **عنوان JWKS الإنتاجي والأصل مجهولان** (ضاق نطاقه ثانيةً) | المُصدِر حُسم ومضبوط افتراضياً، و`aud` حُسم بأنه غير موجود، **وأسماء رسائل المصافحة حُسمت في 7 سبتمبر**. يبقى — وتحقّقتُ منه في 15 سبتمبر — أن `.env.example` ما يزال يحمل `B2CORE_JWKS_URL=https://api.example.com/.well-known/jwks.json` وهو عنوان وهمي، و`B2CORE_ORIGIN=https://portal.example.com` كذلك. والمُصدِر بمساره `/srvsz/auth/clients/v1/` يوحي بأن JWKS ليس في جذر `api.*` |

### حاجزة تشغيلياً — تمنع تشغيلاً آمناً لا الإقلاع

| # | الحاجز | التفصيل |
| --- | --- | --- |
| **B10** | **مُحدِّدات المعدّل ليست ضوابط أمنية** | نوافذ ثابتة في الذاكرة المؤقتة، وكلاهما **يفشل مفتوحاً** عمداً. خلف وكيل، مُحدِّد البوابة لا يزيد جودةً عن `X-Forwarded-For`. موثَّق كسقف تكلفة لا كضابط |
| **B11** | **حدود المبالغ مؤقّتة** | نشر بحدّ أعلى 100,000 دولار للطلب الواحد قرار بحدّ ذاته |
| **B12** | **لم تُعرض في إطار B2CORE حقيقي** (ضاق نطاقه) | شاشة العميل عُرضت وشُغِّلت في Chrome على localhost في 4 سبتمبر، فما بقي هو ما يعتمد على المضيف تحديداً: زر النسخ قد يفشل إن لم يمنح المضيف `clipboard-write`؛ ارتفاع الإطار على طلب طويل؛ لوحة المفاتيح الرقمية. **وأُضيف إليه في 7 سبتمبر ما هو أهمّ:** المصافحة صارت مكتوبة على بروتوكول أكّده طه، ومغطّاة بـ27 اختباراً — **لكن مقابل مضيف مُقلَّد**. أوّل `postMessage` حقيقي بين هذا الإطار وB2CORE لم يقع بعد |
| ~~**B14**~~ ✅ | **مُغلق في `f104780`:** `HistoryTestCase` يوزّع المراجع من عدّاد يبدأ عند 30001، بعيداً عن كل رقم يبحث عنه الملف. ما يلي هو الوصف الأصلي، للتاريخ. **المجموعة متذبذبة بنحو 2% — بند جديد، 15 سبتمبر** | ثلاثة تأكيدات في `apps/finance/test_history.py` (الأسطر 280، 299، 356) تفترض أن مرجعاً عشوائياً من خمس خانات لن يحوي `"250"` أو `"777"`. 280 مرجعاً من 90,000 تحوي الأولى و261 تحوي الثانية. **ليس حاجزاً اليوم لأن لا CI؛ ويصير حاجزاً في اليوم الذي يوجد فيه** — بناء من كل خمسين يفشل بلا سبب، وفريق يتعلّم أن الأحمر لا يعني شيئاً هو فريق بلا بوّابة. يُصلَح **قبل** B2 لا بعده، وثمنه تثبيت المراجع في ثلاثة اختبارات |
| **B13** | **`.env` يحتوي أسراراً** | `.gitignore` يستثنيه ولا يظهر في `git ls-files` — تحقّقتُ بعد إنشاء المستودع. تبقى الفجوة: لا آلية لإدارة الأسرار في الإنتاج |

### ليست حاجزة

- تصدير سجل التدقيق، رفع الإثبات خارج `pay`، حبيبة شارة عدم القراءة، جدول ساعات لكل يوم.
- تحذيرا `check --deploy` الوحيدان على إعدادات الإنتاج (`core.W010`، `core.W011`) — كلاهما إعداد
  بيئة لا كود، وكلاهما مغطّى في B5 وB8 أعلاه.

---

## 5. نتائج التشغيل

كل الأوامر شُغِّلت في `C:\Dev\maxpay-portal` في **15 سبتمبر 2026** عبر `.venv\Scripts\python.exe`
على Python 3.13.5 / Django 5.2.6، على الشجرة النظيفة عند `b52a8c0`.

### 5.1 مجموعة اختبارات Django

```
.venv/Scripts/python.exe manage.py test --verbosity=2
```

```
Found 1248 test(s).
Creating test database for alias 'default' ('file:memorydb_default?mode=memory&cache=shared')...
System check identified no issues (0 silenced).
...
FAIL: test_by_amount_in_dollars (apps.finance.test_history.QueueSearchTests.test_by_amount_in_dollars)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "apps\finance\test_history.py", line 280, in test_by_amount_in_dollars
    self.assertEqual(self.refs_for("250"), {self.second.public_ref})
AssertionError: Items in the first set but not the second:
'MP-20250'

Ran 1248 tests in 1140.784s

FAILED (failures=1)
```

**⚠️ 1247 نجحت وواحد أخفق — والإخفاق ليس انحداراً، بل اختبار متذبذب (flaky) كان كامناً منذ
البداية وظهر اليوم بالصدفة.**

هذه **أهمّ نتيجة في هذا الجرد**، ولم تكن معروفة من قبل. الجرد الأول قال «1187/1187»، ورسالة
الإيداع `f12a019` قالت «1248 tests pass». كلاهما كان صادقاً في حينه، **وكلاهما غير قابل
لإعادة الإنتاج.**

#### لماذا أخفق، ولماذا كان سيخفق في أي يوم آخر

`generate_public_ref()` في `apps/transactions/models.py:100` يبني المرجع من رقم **عشوائي**
من خمس خانات: `MP-` + `secrets.randbelow(...)`. والعشوائية مقصودة وصحيحة — المواصفة §5 تريد
مرجعاً لا يفشي حجم العمليات للتاجر.

و`apps/finance/search.py:query()` يضمّ `Q(public_ref__icontains=term)` **دائماً**، ثم يضيف
مطابقة المبالغ إن كان المصطلح رقماً. وهذا أيضاً صحيح ومقصود: «صندوق واحد لا أربعة».

الاثنان صحيحان، والاختبار هو ما يفترض ما لا يملك: `refs_for("250")` يجب أن يساوي طلباً واحداً
بالضبط. في تشغيل اليوم حصل طلب آخر في التجهيز على المرجع `MP-20250` — **الذي يحوي «250»** —
فطابق على المرجع لا على المبلغ، وسقط التأكيد.

ثلاثة تأكيدات تحمل العيب نفسه:

| الاختبار | السطر | المصطلح | احتمال الإخفاق في التشغيل الواحد |
| --- | --- | --- | --- |
| `test_by_amount_in_dollars` | `test_history.py:280` | `"250"` | **0.62%** |
| `test_by_the_amount_originally_asked_for` | `test_history.py:299` | `"777"` | **0.87%** |
| `test_amount_search_still_works_without_it` | `test_history.py:356` | `"250"` | **0.62%** |

الحساب دقيق لا تقديري: من بين المراجع الـ90,000 الممكنة، 280 تحوي «250» و261 تحوي «777»،
ومُجهِّز `HistoryTestCase` يبني ثلاثة طلبات (أربعة في الاختبار الثاني). **المحصّلة: نحو 2.1%
احتمال أن تُخفق المجموعة في أي تشغيل كامل** — أي تشغيل من كل خمسين تقريباً.

أعدتُ تشغيل الصنف وحده ثلاث مرّات بعد الإخفاق: `OK` في الثلاث. وهذا بالضبط ما يجعل التذبذب
خطيراً — يُقرأ كصدفة عابرة، فيُعاد التشغيل، فيُنسى.

#### لماذا يستحق هذا أن يُسجَّل حاجزاً لا ملاحظة

ما دامت المجموعة تُشغَّل يدوياً (B2)، فتذبذب بنسبة 2% يمرّ بلا أثر: يعيد أحدهم التشغيل ويمضي.
**لكن اللحظة التي يوجد فيها CI هي اللحظة التي يبدأ فيها الضرر**: بناء من كل خمسين يفشل بلا
سبب، وأسرع ما يتعلّمه فريق من ذلك هو أن الأحمر لا يعني شيئاً. أرخص وقت لإصلاح هذا هو **قبل**
كتابة أول ملف CI، لا بعده.

والإصلاح ليس في الكود: `generate_public_ref` و`query()` كلاهما يفعل ما يجب. الاختبارات
الثلاثة هي ما يجب أن يثبّت مراجعه بدل أن يقامر عليها.

ملاحظات أخرى على التشغيل:

- استغرق **1140.8 ثانية (19 دقيقة)**. السبب كما كان: المشروع لا يعرّف `PASSWORD_HASHERS`
  سريعاً لبيئة الاختبار، فكل مستخدم اختبار يمرّ بـ PBKDF2 كاملاً. إضافة `MD5PasswordHasher`
  لإعدادات الاختبار وحدها تختصر هذا إلى دقائق معدودة — وهو ما سيهم كثيراً حين يوجد CI (B2).
  **وهو يضاعف ثمن التذبذب**: كل إعادة تشغيل تكلّف 19 دقيقة.
- **+61 اختباراً منذ الجرد الأول** (كانت 1187): الزيادة من `test_navigation.py` بعد شاشة
  التأليف والقوائم المنسدلة، ومن `test_tokens.py` و`test_checks.py` و`test_session.py` بعد
  إصلاحات التوكن.
- الأسطر التي تظهر كتحذيرات في المخرجات (`B2CORE token rejected: …`، `JWKS lookup failed…`،
  `Service Unavailable: /portal/session/`) هي **سجلّات مقصودة** من اختبارات الرفض، لا إخفاقات.
- التشغيل على **SQLite** (في الذاكرة هذه المرّة، لا `smoke.sqlite3`)، وهو ما يوصّفه `README`
  نفسه بأنه «smoke-test convenience only». لا PostgreSQL ولا Docker في هذه البيئة — تحقّقتُ:
  `docker` و`psql` غير موجودين والمنفذ 5432 مغلق — فتعذّر تشغيلها على المحرّك المستهدَف (B3).

### 5.2 مجموعة اختبارات Node — جديدة، لم تكن في الجرد الأول

```
node --test "tests/js/**/*.test.js"
```

```
ℹ tests 27
ℹ pass 27
ℹ fail 0
ℹ skipped 0
ℹ duration_ms 134.2379
```

**✅ نجحت — 27/27 في 134 مللي ثانية.** Node v24.14.0، بلا `node_modules` وبلا خطوة بناء.

تغطّي `static/js/embed.js` الحقيقي: ترتيب الإرسال، الرفض بـ`embed-jwt-token-error`، الأشكال
الثلاثة التي يصل بها `expiresAt`، والرسائل التي تأتي من الأصل الصحيح والنافذة الخاطئة.
**وهي بوّابة لا يشغّلها شيء آلياً** — انظر B2.

### 5.3 ruff

```
.venv/Scripts/ruff.exe check apps config manage.py
```

```
All checks passed!
```

**✅ نظيف.** الإعداد: `line-length=110`، `target-version=py313`، قواعد `E,F,I,UP,B`،
والهجرات مستثناة.

### 5.4 makemigrations --check

```
.venv/Scripts/python.exe manage.py makemigrations --check --dry-run
```

```
No changes detected
```

**✅ لا هجرات معلّقة.** النماذج والهجرات متزامنة. المجموع **21 ملف هجرة عبر 5 تطبيقات**
(`accounts` 2، `core` 3، `merchants` 7، `rates` 1، `transactions` 8).

> **تصحيح:** الجرد الأول قال «25 ملف هجرة عبر 6 تطبيقات». عددتُها ملفاً ملفاً هذه المرّة:
> 21 عبر 5. لم تُضَف ولم تُحذَف هجرة واحدة منذ 4 سبتمبر، فالرقم السابق كان خطأ عدٍّ لا تغيّراً.

### 5.5 manage.py check

```
.venv/Scripts/python.exe manage.py check
```

```
System check identified no issues (0 silenced).
```

**✅ نظيف.**

### 5.6 manage.py check --deploy --database default

شُغِّل مرتين، لأن النتيجة تختلف جذرياً بحسب وحدة الإعدادات.

**على `config.settings.dev` (الافتراضي المحلي): 10 مشاكل**، كلها متوقّعة ولا معنى لها خارج
سياق الإنتاج:

```
core.W010     CACHES['default'] is LocMemCache, which is per-process.
core.W011     BACKUP_DIR is unset.
core.W015     SECRET_KEY is only 33 characters.
portal.W013   PORTAL_SESSION_COOKIE_SAMESITE='lax' will not be sent inside the B2CORE iframe.
security.W004 SECURE_HSTS_SECONDS not set.
security.W008 SECURE_SSL_REDIRECT not True.
security.W009 SECRET_KEY has less than 50 characters.
security.W012 SESSION_COOKIE_SECURE not True.
security.W016 CSRF_COOKIE_SECURE not True.
security.W018 DEBUG is True.
```

**على `config.settings.prod` (الوضع الحقيقي للنشر):**

```
DJANGO_SETTINGS_MODULE=config.settings.prod \
DJANGO_SECRET_KEY=<64 حرفاً> DJANGO_ALLOWED_HOSTS=maxpay.example.com \
.venv/Scripts/python.exe manage.py check --deploy --database default
```

```
WARNINGS:
?: (core.W010) CACHES['default'] is LocMemCache, which is per-process.
?: (core.W011) BACKUP_DIR is unset, so `manage.py backup_database` writes beside the code.

System check identified 2 issues (0 silenced).
```

**✅ نجح — تحذيران فقط، وكلاهما إعداد بيئة لا عيب في الكود.** نتيجة مطابقة تماماً لتشغيل
4 سبتمبر: إعدادات الإنتاج تجتاز كل فحوص Django الأمنية القياسية وكل الفحوص المخصّصة في
`apps/core/checks.py` و`apps/portal/checks.py`، بما فيها فحص مُشغِّلات سجل التدقيق
`core.W020`/`core.E021` — الذي مرّ هنا **على SQLite فقط** — و`portal.E005`/`portal.E006`
الجديدين بعد إصلاحات 4 سبتمبر.

التحذيران هما بالضبط B5 وB8 في القسم 4، وكلاهما يُغلق بإعداد بيئة النشر:

| التحذير | ما يُغلقه | التحذير المصاحب |
| --- | --- | --- |
| `core.W010` | ضبط `REDIS_URL` | لكن حزمة `redis` **غير مثبَّتة** — يجب إضافتها إلى `requirements.txt` أولاً وإلا انهار التطبيق |
| `core.W011` | ضبط `BACKUP_DIR` على وحدة تخزين لا تشارك نطاق فشل قاعدة البيانات، وجدولة الأمر | الأمر لا يشفّر ولا يشحن الملف خارج المضيف |

### 5.7 ملخّص النتائج

| الفحص | النتيجة | مقارنةً بـ4 سبتمبر |
| --- | --- | --- |
| `manage.py test` (SQLite) | ⚠️ **1247 نجحت، 1 أخفق** (متذبذب)، 1140.8 ثانية | +61 اختباراً، **والتذبذب مكتشَف لأول مرّة** |
| `node --test tests/js` | ✅ 27 نجحت، 0 أخفقت، 0.13 ثانية | **جديد كلياً** |
| `ruff check apps config manage.py` | ✅ All checks passed | بلا تغيير |
| `manage.py makemigrations --check` | ✅ No changes detected | بلا تغيير |
| `manage.py check` | ✅ 0 مشاكل | بلا تغيير |
| `manage.py check --deploy` (dev) | ⚠️ 10 تحذيرات — متوقّعة في التطوير | بلا تغيير |
| `manage.py check --deploy` (prod) | ⚠️ تحذيران — `core.W010`، `core.W011` | بلا تغيير |
| `manage.py test` (PostgreSQL) | ⛔ **لم يُشغَّل** — لا PostgreSQL ولا Docker في البيئة | بلا تغيير |

**الخلاصة:** كل بوّابة ساكنة خضراء ونظيفة. البوّابة الوحيدة التي تهمّ للإنتاج — PostgreSQL —
ما تزال لم تُشغَّل مرّة واحدة. **والمجموعة التي يُستند إليها في كل ذلك ليست حتمية**، وهو ما لم
يكن معروفاً قبل اليوم.

---

## 6. أقصر طريق إلى الإنتاج

بالترتيب، لأن كل بند يعتمد على ما قبله:

1. ~~**`git init` وإيداع أول**~~ — **تمّ** (B1 مُغلق).
2. **دفع `portal/one-screen-compose` إلى `origin` ودمجه** (B1-b). دقيقتان، ويغلق البند الوحيد
   الذي أُضيف إلى قائمة الحواجز منذ الجرد الأول. كل ما تحته يفترض أن الكود ليس على قرص واحد.
3. ~~**إضافة `redis` وخادم WSGI إلى `requirements.txt`**~~ — **تمّ** (`redis` في `f104780`،
   و`gunicorn==26.2.0` في 23 سبتمبر). B5 مُغلق؛ B4 ضاق ولم يُغلق.
4. **رفع PostgreSQL وتشغيل `migrate` ثم المجموعة كاملة عليه** (B3). هذه أول لحظة تُنفَّذ فيها
   مُشغِّلات `plpgsql` لسجل التدقيق.
5. **تصحيح الأسطر القديمة الثلاثة الباقية في `README.md`** (§1.2 بنود 1 و2 و3؛ البند 4 أُغلق) —
   التوثيق يقلّل من قيمة ما بُني، وأول سطر في الملف ما يزال يقول «steps 1–9».
6. ~~**تثبيت المراجع في الاختبارات الثلاثة المتذبذبة**~~ — **تمّ في `f104780`** (B14) — `test_history.py` الأسطر 280
   و299 و356. نصف ساعة عمل، **ويجب أن تسبق الخطوة التالية**: أوّل ما يفعله CI بمجموعة
   متذبذبة هو تعليم الفريق تجاهل اللون الأحمر.
7. **CI يشغّل الأربعة: `test` و`check --deploy --database default` و`ruff` و`node --test`**
   على PostgreSQL (B2). يُستحسن إضافة hasher سريع لإعدادات الاختبار أولاً — المجموعة تأخذ
   الآن قرابة ربع ساعة، وهو ثمن يُدفع على كل دفعة.
8. **مكدّس النشر**: Dockerfile، خادم تطبيقات، خدمة ملفات ساكنة، وكيل عكسي (B4).
9. **الحصول على قيم B2CORE الإنتاجية وأول مصافحة حقيقية** (B9، B12، و§3.3 البنود الباقية).
   البروتوكول صار معروفاً؛ ما ينقص عنوان JWKS والأصل، ثم تشغيل واحد مقابل إطار حيّ.
10. **جدولة النسخ الاحتياطي وتجربة استعادة واحدة على الأقل** (B7, B8).
11. **جولة أسئلة واحدة مع المالية** تغطي القسم 3.1 (تأكيد ما نُفِّذ) والقسم 3.2 (ما لم يُسأل عنه).
    مرّ عليها أحد عشر يوماً بلا حركة، وهي البند الوحيد في هذه القائمة الذي لا يعتمد على أي بند آخر
    — يمكن أن يبدأ اليوم بالتوازي مع كل ما سبق.
12. **اختبار الاختراق** (B6).
